	CREATE USER ORCLSTUDY IDENTIFIED BY ORACLE;
	GRANT CREATE SESSION TO ORACLSTUDY;
	
	CONN ORCLSTUDY/ORACLE

사용자 추가
사용자 권한 변경

세션과 읽기 일관성의 의미
1. 세션이란?
	- 접속 연결 부터 종료 까지
	- 세션간 독립성 유지 
		
	
2. 읽기 일관성 // 시험

3. LOCK




CREATE TABLE MEMBER (

USER_NO NUMBER(11) CONSTRAINT PK_MB PRIMARY KEY,
USER_ID VARCHAR2(20) CONSTRAINT MB_ID_UQ_NN UNIQUE NOT NULL,
USER_PW VARCHAR2(65) CONSTRAINT MB_PW_UQ_NN NOT NULL,
USER_NM VARCHAR2(20) CONSTRAINT MB_NM_UQ_NN NOT NULL,
EMAIL VARCHAR2(60) CONSTRAINT MB_EMAIL_NN NOT NULL,
MOBILE VARCHAR2(11),
REG_DT DATE DEFAULT SYSDATE

);

CREATE SEQUENCE SEQ_MEMBER;

SELECT * FROM USER_CONS_COLUMNS WHERE TABLE_NAME='MEMBER';